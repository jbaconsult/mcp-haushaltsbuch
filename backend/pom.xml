<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>de.jbaconsult.haushaltsbuch</groupId>
    <artifactId>haushaltsbuch-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>Haushaltsbuch</name>
    <description>Ein Haushaltsbuch, dessen primäre Schnittstelle das Gespräch ist</description>

    <modules>
        <module>kern</module>
        <module>persistenz</module>
        <module>mcp</module>
        <module>api</module>
        <module>app</module>
    </modules>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <maven.compiler.release>21</maven.compiler.release>

        <!-- Quarkus 3.33 ist die aktuelle LTS-Linie. Die MCP-Extension wird gegen genau
             diese Linie gebaut - Versionen bewusst anheben, nie per Bereichsangabe. -->
        <quarkus.platform.group-id>io.quarkus.platform</quarkus.platform.group-id>
        <quarkus.platform.artifact-id>quarkus-bom</quarkus.platform.artifact-id>
        <quarkus.platform.version>3.33.3.1</quarkus.platform.version>

        <!-- SSE-Transport aus der stabilen 1.x-Linie. Die 2.x-Linie bringt Streamable
             HTTP, steht aber noch bei Release Candidate. Siehe ADR-0001. -->
        <quarkus-mcp-server.version>1.13.1</quarkus-mcp-server.version>

        <!-- Nicht im Quarkus-BOM enthalten, daher hier gepinnt. Bewusst die stabile 3.x-Linie
             und nicht 4.0.0-M1. -->
        <assertj.version>3.27.7</assertj.version>

        <compiler-plugin.version>3.14.0</compiler-plugin.version>
        <surefire-plugin.version>3.5.4</surefire-plugin.version>
        <failsafe-plugin.version>3.5.4</failsafe-plugin.version>
        <jandex-plugin.version>3.6.0</jandex-plugin.version>
        <spotless-plugin.version>3.10.0</spotless-plugin.version>

        <skipITs>false</skipITs>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>${quarkus.platform.group-id}</groupId>
                <artifactId>${quarkus.platform.artifact-id}</artifactId>
                <version>${quarkus.platform.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>io.quarkiverse.mcp</groupId>
                <artifactId>quarkus-mcp-server-bom</artifactId>
                <version>${quarkus-mcp-server.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <dependency>
                <groupId>org.assertj</groupId>
                <artifactId>assertj-core</artifactId>
                <version>${assertj.version}</version>
                <scope>test</scope>
            </dependency>

            <dependency>
                <groupId>de.jbaconsult.haushaltsbuch</groupId>
                <artifactId>kern</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>de.jbaconsult.haushaltsbuch</groupId>
                <artifactId>persistenz</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>de.jbaconsult.haushaltsbuch</groupId>
                <artifactId>mcp</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>de.jbaconsult.haushaltsbuch</groupId>
                <artifactId>api</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${compiler-plugin.version}</version>
                    <configuration>
                        <parameters>true</parameters>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${surefire-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-failsafe-plugin</artifactId>
                    <version>${failsafe-plugin.version}</version>
                </plugin>
                <!-- Quarkus findet CDI-Beans in einem Abhängigkeits-Modul nur, wenn dieses
                     einen Jandex-Index mitbringt. Fehlt er, startet die Anwendung zwar,
                     aber die Beans aus dem Modul fehlen - der Fehler zeigt sich erst zur
                     Laufzeit als unerklärliche UnsatisfiedResolutionException. -->
                <plugin>
                    <groupId>io.smallrye</groupId>
                    <artifactId>jandex-maven-plugin</artifactId>
                    <version>${jandex-plugin.version}</version>
                    <executions>
                        <execution>
                            <id>make-index</id>
                            <goals>
                                <goal>jandex</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <version>${spotless-plugin.version}</version>
                <configuration>
                    <java>
                        <palantirJavaFormat/>
                        <removeUnusedImports/>
                        <importOrder>
                            <order>java,javax,jakarta,,de.jbaconsult</order>
                        </importOrder>
                        <trimTrailingWhitespace/>
                        <endWithNewline/>
                    </java>
                </configuration>
                <executions>
                    <execution>
                        <!-- Im normalen Build nur prüfen. Formatieren auf Zuruf über
                             "mvn spotless:apply" bzw. "make formatieren". -->
                        <id>spotless-check</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
