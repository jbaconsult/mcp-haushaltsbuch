<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>de.jbaconsult.haushaltsbuch</groupId>
        <artifactId>haushaltsbuch-parent</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>app</artifactId>
    <name>Haushaltsbuch :: App</name>
    <description>
        Quarkus-Runner. Bündelt die Module und hält die Konfiguration - keine eigene
        Fachlogik. Hier liegen die Integrationstests, die den ganzen Stapel brauchen.
    </description>

    <dependencies>
        <dependency>
            <groupId>de.jbaconsult.haushaltsbuch</groupId>
            <artifactId>persistenz</artifactId>
        </dependency>
        <dependency>
            <groupId>de.jbaconsult.haushaltsbuch</groupId>
            <artifactId>mcp</artifactId>
        </dependency>
        <dependency>
            <groupId>de.jbaconsult.haushaltsbuch</groupId>
            <artifactId>api</artifactId>
        </dependency>
        <!--
          Der Adapter zum Bankanbieter wird hier gebunden und nirgends sonst. kern
          kennt nur den Port; welcher Anbieter ihn bedient, entscheidet der Runner.
          Ein Wechsel ist damit ein Austausch dieser Zeile plus Konfiguration.
        -->
        <dependency>
            <groupId>de.jbaconsult.haushaltsbuch</groupId>
            <artifactId>bankzugang</artifactId>
        </dependency>

        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-smallrye-health</artifactId>
        </dependency>

        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-junit</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Ohne diese Erweiterung misst JaCoCo die Integrationstests nicht.
             Hibernate und Panache verändern den Bytecode der Entitäten und
             Repositories beim Start; die geladene Klasse stimmt dann nicht mehr
             mit der auf der Platte überein, und JaCoCo verwirft die Messung
             stillschweigend. "persistenz" stand deshalb auf null Prozent, obwohl
             RlsZugriffTest genau dort hineinläuft. -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-jacoco</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>rest-assured</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Startet Postgres für Tests von selbst. Ohne echtes Postgres ist die
             Zugriffskontrolle nicht prüfbar - RLS gibt es in keiner In-Memory-Datenbank. -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-jdbc-postgresql</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>${quarkus.platform.group-id}</groupId>
                <artifactId>quarkus-maven-plugin</artifactId>
                <version>${quarkus.platform.version}</version>
                <extensions>true</extensions>
                <executions>
                    <execution>
                        <goals>
                            <goal>build</goal>
                            <goal>generate-code</goal>
                            <goal>generate-code-tests</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <systemPropertyVariables>
                        <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
                        <maven.home>${maven.home}</maven.home>
                    </systemPropertyVariables>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <skipITs>${skipITs}</skipITs>
                    <systemPropertyVariables>
                        <native.image.path>${project.build.directory}/${project.build.finalName}-runner</native.image.path>
                        <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
                        <maven.home>${maven.home}</maven.home>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
